{
   "version": 2,
   "name": "StaticDelivr",
   "builds": [
      { "src": "server.js", "use": "@vercel/node" }
   ],
   "routes": [
      { "src": "/(.*)", "dest": "/server.js" }
      { "src": "/(.*)", "dest": "/static" }
   ]
}
